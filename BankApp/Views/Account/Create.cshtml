@model UserListModel
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row mt-3">

    <div class="col-6">
        <h3>Kullanıcı Bilgileri</h3>
        <p>
            Kullanıcı Adı: @Model.Name
        </p>
        <p>
            Kullanıcı Soyadı: @Model.Surname
        </p>
        <p>
            Aktif Hesap Sayısı: <getAccountCount application-user-id="@Model.Id"></getAccountCount>
        </p>
    </div>

    <div class="col-6">
        <h4>Ekleme İşlemi</h4>
        
        <form method="post" asp-action="Create" asp-controller="Account">
            <input type="hidden" name="ApplicationUserId" value="@Model.Id">
            <div class="mb-3">
                <label class="form-label">Hesap Numarası :</label>
                <input type="number" class="form-control" name="AccountNumber">
            </div>
            <div class="mb-3">
                <label class="form-label">Bakiye :</label>
                <input type="number" class="form-control" name="Balance">
            </div>
            <div class="mb-3">
                <button class="btn btn-primary">Kaydet</button>
            </div>
        </form>

    </div>

</div>